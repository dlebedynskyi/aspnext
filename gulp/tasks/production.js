var gulp = require('gulp');
var runSequence = require('run-sequence');

/*
gulp.task('production', function (cb) {
    runSequence('clean', //cleaning everything 
    ['build:bundle'],//in parrallel tasks : 1.building and minify app code 
     cb);
})*/